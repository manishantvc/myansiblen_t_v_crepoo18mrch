---
 -hosts: all
 become: yes
 tasks:
  -name: i want to install web server on ansible ubuntu nodes
  apt: 
   name: apache2
   state: absent  
   -name: i want to install webserver on ansible ubuntu nodes